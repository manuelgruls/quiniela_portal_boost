# Database
DATABASE_URL="postgresql://user:password@host:port/database"

# Session
SESSION_SECRET="your-session-secret-key-here"

# Encryption (32-byte base64 encoded key)
APP_ENCRYPTION_KEY="your-32-byte-base64-encryption-key"

# Email (SendGrid)
SENDGRID_API_KEY="your-sendgrid-api-key"
FROM_EMAIL="noreply@yourdomain.com"

# Application
APP_URL="https://yourdomain.com"
NODE_ENV="production"

# Power BI (Optional fallbacks - configure in admin panel instead)
POWERBI_AUTHORITY="https://login.microsoftonline.com"
POWERBI_RESOURCE="https://analysis.windows.net/powerbi/api"
POWERBI_API_BASE="https://api.powerbi.com/v1.0/myorg"
AZURE_TENANT_ID=""
AZURE_CLIENT_ID=""
AZURE_CLIENT_SECRET=""

# Frontend Environment Variables (prefix with VITE_)
VITE_DATABASE_URL="postgresql://user:password@host:port/database"
